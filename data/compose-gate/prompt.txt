You are contributing ONE typed analysis to a shared knowledge map of the
COVID-19 origins dispute. Emit a JSON array of nodes against the contract below,
and nothing else. Emit structure, not advocacy.

TERMINAL TYPES (assign by closure KIND, not confidence):
  measurement | irreducible-prior | question-set | pending
A claim is irreducible-prior ONLY if no shared evidence could resolve it. If a
record that WOULD resolve it exists but is withheld, type it question-set or
pending and name that dataset (it is withheld, not irreducible).

NODE ADDRESSES (file each claim onto the one that fits; do not invent others):
  Q-PIVOT-MARKET  Huanan early-case clustering: origin signal or ascertainment artifact?
  Q-GEO-WHYWUHAN  What does the outbreak surfacing in Wuhan imply, against which reference class?
  Q-FCS-DEFUSE    What do the furin cleavage site and the 2018 DEFUSE proposal jointly imply?
  Q-BASERATE      Prior on a lab-associated vs natural origin.
  Q-METHOD        Do any analyses let degraded data raise confidence?
  Q-LAYER0        How did institutions shape which hypothesis counts as credible?

SOURCE REGISTRY (cite datasets ONLY by these idents; [W] = withheld record):
  DS-EARLYCASE-DEC2019 [W]   Dec-2019 Wuhan early-case location list + search protocol
  DS-ENVSAMPLES-HSM          Huanan environmental / animal-stall samples
  DS-PHYLO-EARLY             Early-genome phylogeny; lineages A and B
  DS-BASERATE-HIST           Historical pandemic-emergence frequency record
  DS-FCS-MOLEC               SARS-CoV-2 furin site sequence + codon usage
  DS-DEFUSE-2018             DEFUSE proposal text
  DS-SARBECO-SURVEY          Catalogued sarbecovirus reference set
  DS-LABRECORDS-WIV [W]      WIV inventory / Sept-2019 offline database / BSL logs

NODE SCHEMA (output a JSON array of objects shaped exactly like this):
{
  "addr": one Q- address above,
  "terminal": one terminal type,
  "claim": "the claim, one sentence",
  "loadBearing": true only for a crux carrying most of the dispute (rare),
  "conjecture": true if speculative/unchecked OR your numbers are fitted not sourced,
  "namesWithheldResolver": a [W] DS- id if a withheld record would resolve this, else null,
  "closureNote": "how it closes; if withheld-gated, name the DS-",
  "readings": [ {"side": "one reading"}, {"side": "the competing reading"} ],
  "sources": [ { "ident": "a DS- id from the registry",
                 "rel": "anchor | concept | framework | illustration | conflicts-with",
                 "typing": "author-verified if you actually checked the source, else author-declared" } ]
}

RULES:
- Use ONLY the addresses and DS- idents above. The gate rejects unregistered ones.
- typing = author-verified ONLY if you truly inspected the source. Do not inflate.
- If a claim rests on a withheld record, do not type it irreducible-prior.
- You do not set an emitter id; the page assigns one. Output the JSON array only.