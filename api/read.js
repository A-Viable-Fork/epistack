// Role: the open read interface: resolve, decompose, compose, compare, dependents, gaps, profile, exclusions, disagreements; no credential.
// Contract: STUB: the read half of api/api.js's own trellis surface, to split out later. The v3
//   read contract (claims with their grounding) is realized in api/client-api.mjs over a provider
//   (Prompt 10); this stub remains the eventual home for splitting api.js's open reads.
// Invariant: api imports kernel and api; never the periphery.
