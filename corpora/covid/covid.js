// Role: the COVID-origin case home. The CANONICAL covid case is the densified origins store,
//   corpora/covid/covid-origins.js: the Rootclaim debate as evidence to floors, the
//   zoonosis/lab-leak contradiction, and the priors as the computed crux.
// Contract: covid-origins.js is the covid case. A SEPARATE population-mismatch pattern demonstration,
//   covid.instance in corpora/population/population-pipeline.js, is kept by design (Option B)
//   as a distinct structural demo (the stage-1 break), not the canonical case; the two do not duplicate.
// Invariant: pure data; corpora imports nothing.
