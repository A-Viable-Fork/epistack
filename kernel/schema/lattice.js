// Role: the grounding lattice: the ordering every claim sits in (ceiling + effective grounding), with meet and join.
// Contract: STUB [1.1] Stage 1, specified not built (docs/status-ledger.md). Carried in the schema; computes the lattice.
// Invariant: pure logic, DOM-free; kernel imports only kernel.
