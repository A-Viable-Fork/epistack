---
Type: record
Purpose: "Records the works-versus-decorates findings the accessible-register authoring pass surfaced in the precise register, as candidates for a human decision to cut or to ground, not an automated edit."
Depends on: [docs/register-view.md]
Depended on by: []
---

# Register Diet Findings

The accessible-register authoring pass (register view, phase B) runs the works-versus-decorates
cut over `docs/what-stands-without-trust.md`, and that cut audits the precise register while it
authors the accessible one. This is the record of what it surfaced: each element that reads as
pure decoration (a figure that does no work and points at no node), and each element marked
load-bearing but grounded nowhere (a forced isomorphism or a homeless truth). It is a findings
record for a human decision, because cutting from or grounding the canonical argument is an
authored act; nothing here is applied automatically, and the precise register is unchanged by
this pass.

The headline is that the document is disciplined: pure decoration is rare, which is what the
design predicted, and the two heavy categories are the ramp and the constitutive figure, both of
which do work and are kept. The findings below are the residue, and they are modest.

## Decoration candidates (do no work, point at no node)

- **`where-it-points` and the appendix, "the way a path becomes a road because people kept
  walking it."** An illustrative simile for standards-from-use. It teaches the intuition and
  points at no node. Verdict for the human: keep as a light teaching figure (it does teaching
  work, which is not nothing) or cut for terseness. Leaning keep, since it earns its length once
  and the concept is genuinely non-obvious. Low priority.

- **`what-was-built`, "a good synthesis would have smoothed flat."** A recurring framing phrase
  across the three case moves. It is doing rhetorical work (setting the counterexample against the
  synthesis foil) rather than asserting a checkable claim, but that work is real and on-thesis, so
  it is not decoration to cut. Recorded for completeness, not flagged.

## Load-bearing but under-grounded (resolved: the full-merge snapshot grounds them finely)

Both grounding findings below are now **resolved**. The gate snapshot
(`build/vendor-snapshot.mjs`) merges the two migrated cases with the three densified case stores
(covid, the densified LHC legs, and the eggs domain stores), so the finer nodes the sentences name
now resolve through the read contract. The `what-was-built` node_links were upgraded to the finer
keys accordingly (`corpora/registers/judges-accessible.js`), and the register view grounds the
reprice on the shared dependency node and the covid verdict on its named prior.

- **`what-was-built` / `kernel-watched-at-work`, the reified shared dependency in the LHC case
  ("one assumption wearing several coats").** Load-bearing: the whole LHC counterexample is that
  three lines read independent share one dependency. **Resolved:** the densified LHC legs are in
  the snapshot, so the shared dependency node ("framework choice") resolves through the read
  contract and the accessible reader can click the reprice to the exact shared node, rather than
  stopping at `lhc.claim` + robustness.

- **`what-was-built`, the COVID priors ("the prior each verdict turns on").** Load-bearing: the
  COVID counterexample is that the verdict turns on a named prior. **Resolved:** the covid-origins
  store is in the snapshot, so the named prior ("Miller's prior") resolves through the contract and
  the move grounds on the prior node the sentence names, rather than on the coarse `covid.instance`
  node.

## What was checked and found sound

The constitutive figures (the definition of knowledge, "the clearing") are load-bearing and are
kept, translated to plainer figures with a delta, not cut. The earned case-figures are
load-bearing and grounded, kept verbatim. The enacted commitments (the opening concession, the
dated postscript) are promoted to stated commitments, not dropped. No forced isomorphism and no
homeless truth of the philosophical kind (a Spinoza-or-Deleuze resonance carrying no weight) was
found, because the document does not lean on that kind of resonance. The diet is therefore short,
and the two grounding findings above, the ones that would most improve the demonstration, have
since been acted on: the full-merge snapshot grounds both the reprice and the covid verdict on
their finer nodes.
